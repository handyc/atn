# build artifacts
*.o
atn

# model / training state (the "brain" and its weight cache)
*.brain
*.brain.weights
*.weights

# compressed outputs
*.atcm
*.atnz

# training data / corpora (not source)
*.htm
*.html
*.txt
*.meta
*.tgz
*.tar.gz
*Zone.Identifier
Hamlet*

# scraped news working tree (data, never source)
newsdata/

# atn-ga evolution run dirs (brains, territory, eval — all derived data)
ga-run/
*.ga/

# local editor / tool config
.claude/

# ...but DO track the text manual (overrides *.txt above)
!MANUAL.txt

# python bytecode
__pycache__/
*.pyc

# demo output
demo-run/
demo-langs/

# web app dev database (regenerate with manage.py migrate + import_run)
web/db.sqlite3
# the global *.html ignore above is for stray test files — keep the app's templates
!web/corpus/templates/corpus/*.html
demo-code/
demo-formal/
demo-meta/
demo-route/

# CA-network session: generated pools, run dirs, logs
julnewt-pool/
cell10-pool/
caga-news/
caga2-news/
caga3-news/
cell10ga-news/
cell10ga2-news/
*-news.log
honest-eval.log
caga2-eval.log
genpool.log
b-experiments.log

alice/*/outputs/
alice/*/watch.log
alice/*/*.log

# never commit credentials
github_pat*
*.pat

# dissemination dir — article drafts, PDFs, figures: ALL local, never in the repo
dissemination/
fig*.png

# transient run logs, job ids, data dumps, bundle-duplicate scripts, GA state
*.log
alice/*/JOBID
alice/**/pool.npy
alice/ga-sweep-*/caca.py
alice/ga-sweep-*/caga.py
caga-code/
caga-langs/


# pact ELF build artifacts (regenerate with build_pactbundle.py + build_pactelf.py)
notesync
notesync.c
pactbundle.html
pactbundle.html.z
pactbundle.html.gz
/notesync_ut
